# When set, backend tries to connect, otherwise stores to local json files
# MONGODB_URI=mongodb://localhost:27017/

# Initial Admin credentials for the web interface (optional — defaults to admin/admin)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin